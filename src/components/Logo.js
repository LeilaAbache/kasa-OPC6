import React from "react";
import logo from "../assets/LOGO.png";

const Logo = () => {
  return (
    <div>
      <img src={logo} alt="kasa" className="logo"></img>
    </div>
  );
};

export default Logo;
